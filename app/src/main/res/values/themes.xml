<resources>
    <style name="Theme.TransportChat" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Black system bars (use only android: names) -->
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
